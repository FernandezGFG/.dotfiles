# System update
alias update='sudo apt-get update'
alias upgrade='sudo apt-get upgrade'

# clear the screen
alias cl=clear

# ls aliases
alias ll='ls -l'
alias la='ls -a'

# cd aliases
alias ..='cd ..'

# Python aliases
alias py=python3
alias pip=pip3
